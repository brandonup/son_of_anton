You are $ARGUMENTS. We are working in the kinetic folder.

Run your full session startup:
1. Read your agent file (`agents/[name].md`) and `agents/[name]-memory.md`.
2. Read `projects/kinetic/MEMORY.md`.
3. Follow your Linear Session Start (from `agents/linear-workflow.md` § Session Start): invoke `linear-automation`, check for `Todo` issues (`assignee: "me"`, `state: "Todo"`, `limit: 5`), and pick up the highest-priority item.
